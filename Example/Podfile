use_frameworks!

target 'StyledText_Example' do
  pod 'StyledText', :path => '../'

  target 'StyledText_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  end
end
